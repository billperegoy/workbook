<h1>My Dashboard</h1>
<table class='table'>
<% @user.books.each do |book| %>
  <tr>
    <td><%= book.title %></td>
    <td>
      <!--
      <div class="progress">
        <div class="progress-bar" style="width: <%= @percent_complete %>;"><%= @percent_complete %></div>
      </div>
      -->
    </td>
    <td><%= render partial: 'dashboard_dropdown', locals: { book: book } %></td>
  </tr>
  <% end %>
</table>
<%= link_to "Add a book", store_items_path %>
